# UI Plan Prompt

Produce a UI implementation plan covering:
- component map
- route impact
- state impact
- touched files list
- reusable UI components

Keep macro and micro views separate.
Keep Skills and Tools visually distinct.
Preserve preview -> diff -> apply -> rollback.
