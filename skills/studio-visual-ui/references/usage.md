# Usage

## Example prompt

- Use $visual-ui to standardize this React app.
- Keep light theme as default.
- Add optional dark theme.
- Use design tokens and reusable components.
- Do not change business logic.

## Execution note

Use files in `assets/templates/react/` as implementation baseline.
